language: java
jdk:
  - openjdk8

branches:
  only:
    - main

# ------------추가 부분----------------

before_script:
  - cd SpringBootAws/

# ------------------------------------

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI 실행 완료 시 메일로 열람
notifications:
  email:
    recipients:
      - "alwn04016@naver.com"
 